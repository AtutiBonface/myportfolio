import { ContainerIntersectionDirective } from './container-intersection.directive';

describe('ContainerIntersectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainerIntersectionDirective();
    expect(directive).toBeTruthy();
  });
});
